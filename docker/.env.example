# =============================================================================
# Magicodex Docker Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
# NEVER commit .env to version control!
# =============================================================================

# =============================================================================
# Database (uses existing PostgreSQL on VPS)
# =============================================================================
# Use one of these formats depending on your setup:
#
# If PostgreSQL listens on localhost (127.0.0.1):
#   DATABASE_URL=postgresql://user:password@host.docker.internal:5432/magicodex
#
# If PostgreSQL listens on all interfaces or specific IP:
#   DATABASE_URL=postgresql://user:password@YOUR_VPS_IP:5432/magicodex
#
# Example with your VPS PostgreSQL:
DATABASE_URL=postgresql://magicodex:YOUR_DB_PASSWORD@host.docker.internal:5432/magicodex

# =============================================================================
# JWT Secrets (generate with: openssl rand -base64 64)
# =============================================================================
JWT_SECRET=CHANGE_ME_GENERATE_SECURE_SECRET
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_ANOTHER_SECRET

# =============================================================================
# Frontend URL (for CORS)
# =============================================================================
FRONTEND_URL=https://magicodex.yourdomain.com

# =============================================================================
# Scryfall API
# =============================================================================
SCRYFALL_DELAY_MS=100
